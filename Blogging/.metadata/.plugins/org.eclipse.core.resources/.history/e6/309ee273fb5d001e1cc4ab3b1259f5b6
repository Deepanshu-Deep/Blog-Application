package com.deep;

import java.util.Arrays;
import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;


public class Practice {

	public static void main(String[] args) {
		
		
//		List<String> list = Arrays.asList("Deepanshu","Anpu", "Vidyanshu", "Priyanshu", "Subhadra", "Pradeep");
//		
//		List<String> li = list.stream().filter(e -> e.endsWith("u")).collect(Collectors.toList());
//		
//		
//		System.out.println(li);
		
		
		List<Integer> li = List.of(12, 41, 13, 55, 65);
		
		int sum = li.stream().mapToInt(Integer::intValue).sum();

//		List<Integer> list = li.stream().map(e -> e*e).collect(Collectors.toList());
		
//		long count = li.stream().count();
//		
//		
		System.out.println(sum);
		
		
	}
	
	
	
	
	
}
